apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: tickets-hall
  labels:
    app: customers-service
spec:
  selector:
    app: customers-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
