apiVersion: v1
kind: Service
metadata:
  name: postgres-customers
  namespace: tickets-hall
  labels:
    app: postgres-customers
spec:
  selector:
    app: postgres-customers
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
