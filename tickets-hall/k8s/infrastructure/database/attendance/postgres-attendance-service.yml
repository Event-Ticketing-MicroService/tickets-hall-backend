apiVersion: v1
kind: Service
metadata:
  name: postgres-attendance
  namespace: tickets-hall
  labels:
    app: postgres-attendance
spec:
  selector:
    app: postgres-attendance
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP