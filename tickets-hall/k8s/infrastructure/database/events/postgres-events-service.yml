apiVersion: v1
kind: Service
metadata:
  name: postgres-events
  namespace: tickets-hall
  labels:
    app: postgres-events
spec:
  selector:
    app: postgres-events
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
