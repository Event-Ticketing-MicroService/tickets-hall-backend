apiVersion: v1
kind: Service
metadata:
  name: tickets-service
  namespace: tickets-hall
  labels:
    app: tickets-service
spec:
  selector:
    app: tickets-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
