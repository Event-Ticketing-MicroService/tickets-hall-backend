apiVersion: v1
kind: Service
metadata:
  name: events-service
  namespace: tickets-hall
  labels:
    app: events-service
spec:
  selector:
    app: events-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
